# Copy to .env.local (gitignored). Dev-only unless packaged with resources/.env.local.

# Google OAuth (desktop PKCE) — required for onboarding sign-in
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=

# Agent behaviour flags (defaults match production)
# VISION_MODE=subagent
# CONTROL_BRAIN=openai-cua
# VISION_MODEL=gpt-4o
# WEB_SEARCH_MODEL=gpt-5.5
# COMPUTER_USE_MODEL=gpt-5.5
# REALTIME_VOICE=marin
